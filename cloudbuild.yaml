steps:
  - name: 'gcr.io/kaniko-project/executor:latest'
    args:
      - '--dockerfile=Dockerfile'
      - '--destination=gcr.io/$PROJECT_ID/support-gateway'
      - '--cache=true'
      - '--cache-ttl=168h'
      - '--snapshot-mode=redo'
